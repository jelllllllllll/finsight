Deployment quick notes:
- Vercel: point to the frontend folder in the repo; set environment variable VITE_API_URL to your backend URL.
- Render: point to the backend folder and set environment variables MONGO_URI and JWT_SECRET.
