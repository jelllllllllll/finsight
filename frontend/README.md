# FinSight Frontend

## Setup
1. Install dependencies:
   ```
   npm install
   ```
2. Run dev server:
   ```
   npm run dev
   ```

## Env
Set VITE_API_URL to point to backend, for example:
VITE_API_URL=http://localhost:5000/api
